# Python agent

This directory is reserved for the FastAPI-based prototype agent.

The agent proposes structured changes and returns diffs for review. File writes,
patch application, and command execution remain the responsibility of the Tauri
Rust backend after explicit user approval.
